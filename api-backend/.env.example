# ============================================================================
# BANELO POS API - ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git (it's in .gitignore)

# Server Configuration
NODE_ENV=production
PORT=3000

# PostgreSQL Database Configuration (Render)
# Get these values from your Render PostgreSQL dashboard
DATABASE_URL=postgres://username:password@hostname:5432/banelo_db

# Alternative: Individual connection parameters (if not using DATABASE_URL)
DB_HOST=your-render-hostname.oregon-postgres.render.com
DB_PORT=5432
DB_NAME=banelo_db
DB_USER=your_render_username
DB_PASSWORD=your_render_password

# Database Pool Settings
DB_MAX_CONNECTIONS=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# CORS Settings (optional - add your frontend URLs)
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# ============================================================================
# RENDER DEPLOYMENT NOTES:
# ============================================================================
# 1. In Render dashboard, go to your Web Service
# 2. Navigate to "Environment" tab
# 3. Add these environment variables (Render auto-provides DATABASE_URL)
# 4. DATABASE_URL is automatically set by Render when you link PostgreSQL
# ============================================================================
